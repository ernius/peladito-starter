# App
PORT=3000

# Database (matchea docker-compose.yml de la raíz)
DATABASE_HOST=localhost
DATABASE_PORT=5435
DATABASE_USER=peladito
DATABASE_PASSWORD=peladito
DATABASE_NAME=peladito

# Auth
JWT_SECRET=cambiame-por-un-secreto-largo
JWT_EXPIRES_IN=7d

# AI providers (semana 2 — nunca commitear valores reales)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
